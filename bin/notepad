#!/bin/bash
# Universal notepad command: open Notepad++ with passed parameters.
if command -v notepad++ >/dev/null 2>&1; then
  "/home/anon/.wine/drive_c/Program Files (x86)/notepad++" "$@"
else
  echo "Notepad++ is not installed or not in PATH."
  exit 1
fi
